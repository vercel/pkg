'use strict';

module.exports = {
  pkgConfig: {
    assets: [
      // TODO look at exceljs and implement as
      // many __dirname use cases as possible
      'lib/xlsx/xform/core/*.xml'
    ]
  }
};
