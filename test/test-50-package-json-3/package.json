{
  "bin": "test-x-index.js",
  "pkg": {
    "patches": {
      "test-x-index.js": [
        {
          "command": "replace",
          "from": "require(__dirname + '/' + dataPath);",
          "to": "require(process.cwd() + '/' + dataPath);"
        }
      ]
    }
  }
}
